import 'package:flutter/material.dart';

class ExerciseTickOptionsWidget extends StatelessWidget {
  const ExerciseTickOptionsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
